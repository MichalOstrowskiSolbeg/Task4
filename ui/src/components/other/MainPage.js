function MainPage() {
    return (
        <main>
            <h1>
                Welcome
            </h1>
            <p>This application allows you to create your own reading list from available books in database.</p>
            <p>You can add book to your list, set category and mark(using switch button) if you have already read it.</p>
        </main>
    )
}

export default MainPage;